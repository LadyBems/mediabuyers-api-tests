# Per-environment configuration. Copy to .env and adjust per target environment.
# There is no live server for this task; the placeholder mirrors how a real
# deployment would resolve the base URL from configuration.
BASE_URL=https://staging.example.com
